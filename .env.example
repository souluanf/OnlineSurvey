# PostgreSQL
POSTGRES_DB=onlinesurvey
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Connection String (used by ASP.NET Core)
ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=onlinesurvey;Username=postgres;Password=postgres

# CORS Allowed Origins (comma-separated, used by ASP.NET Core)
AllowedOrigins__0=http://localhost:5000
AllowedOrigins__1=http://localhost:5173